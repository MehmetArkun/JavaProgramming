package day41_Exceptions;

public class ErrorsIntro {

    public static void main(String[] args) {

        // indicates an illegal operation is being performed
        //occurs during the runtime only
        //they can not be recovered, and not recommended to handle them
        //one of the most common one is out of memory one
        //you can not handle it via changing the code for instance
        //the one that we are meant to handle is exception, not errors
        //errors can happen one in a thousand times
        //OutOfMemory error means heap memory is full
    }
}
