package day41_Exceptions;

public class InterviewQuestionFinallyFInalFinalize {

    public static void main(String[] args) {
        /*
        difference between final keyword finalize method and finally block

        -final is a keyword that defines classes, variables and methods that prevents them from being changed again
        -finalize method is a method used by garbage collector to remove that object from heap memory. every object has it.
        -finally is a block that is used within the structure of try&catch block to handle exceptions

         */
    }
}
