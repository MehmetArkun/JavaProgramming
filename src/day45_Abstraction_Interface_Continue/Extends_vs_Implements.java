package day45_Abstraction_Interface_Continue;

public class Extends_vs_Implements {
    public static void main(String[] args) {

        //EXTENDS:
        //multiple inheritance is NOT allowed between classes = there is only one parent
        //multiple inheritance is allowed between interfaces = there can be various parents, interfaces are bastards
        // class extends class
        //interface extends interfaces
        //if you use extends and implements together, first you gotta use extends, then implements

    }
}
